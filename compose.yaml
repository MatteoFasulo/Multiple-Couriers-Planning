version: '3.9'
services:
  cdmo:
    build:
      context: .
    command: python ......
    volumes:
      - .:/app
    container_name: cmdo_project
    network_mode: "host"
    image: multiple-couriers-planning-cdmo:latest
